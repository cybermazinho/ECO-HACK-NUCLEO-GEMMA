# ambiente virtual — recriar com: python3 -m venv venv && ./venv/bin/pip install -r requirements.txt
venv/
__pycache__/
*.pyc

# dados de execução — não versionar (banco local e uploads de pacientes)
nucleo.db
uploads/

# diversos
.DS_Store
*.log
